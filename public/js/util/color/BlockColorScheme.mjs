export default class BlockColorScheme {
  constructor(center, top, right, bottom, left) {
    this.center = center;
    this.top = top;
    this.right = right;
    this.bottom = bottom;
    this.left = left;
  }
}
